--- artifacts/api-server/src/routes/modules/ai.ts
+++ artifacts/api-server/src/routes/modules/ai.ts
@@ -298,21 +298,14 @@

     If no food or medical info is visible, return {"error": "No recognized items found."}`;

-    let payload: any = [{
+    const payload: import("../../lib/ai").AIMessage[] = [{
       role: "user",
-      content: [
-        { type: "text", text: prompt },
-        { type: "image_url", image_url: { url: `data:${mimeType};base64,${imageBase64}` } }
-      ]
+      content: prompt,
+      media: {
+        mimeType,
+        data: imageBase64
+      }
     }];
-
-    try {
-      const [config] = await db.select().from(aiConfigTable).where(eq(aiConfigTable.feature, "smart_scan")).limit(1);
-      if (config && config.provider === "google") {
-        payload = [{ role: "user", parts: [{ text: prompt }, { inlineData: { mimeType, data: imageBase64 } }] }];
-      }
-    } catch (e) {}

     let jsonStr;
     try {
